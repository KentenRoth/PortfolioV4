import React from 'react';
import Nav from '../Components/nav';

const Projects = () => {
	return (
		<div className="projects">
			<Nav page={'Projects'} />
		</div>
	);
};

export default Projects;
